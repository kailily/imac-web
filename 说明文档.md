# 娜梅利亚 · IMAC 国际异常管理联盟 网站原型

> 一个深色系、SCP 风格的虚构组织官网原型：「国际异常管理联盟（International Anomaly Management Coalition）」公民安全门户。
> 本包由飞书「秒搭」设计稿导出，已做本地化处理（React/Babel 已内置，可离线运行），文件全部为 UTF-8 编码。

---

## 一、先打开看看（不用装任何东西）

三种方式任选：

| 方式 | 操作 | 说明 |
|---|---|---|
| ① 双击预览 | 双击 `预览-双击打开.html` | 单文件离线版，所有代码已内联，双击即开，断网也能看。**注意：它是打包快照，改代码后不会自动更新** |
| ② 一键启动 | 双击 `一键预览.bat` | 自动检测 8090 端口、启动本地服务器并打开浏览器，访问 http://127.0.0.1:8090/；重复双击不会重复起服务 |
| ③ 双击启动 | 双击 `启动网页.bat` | 自动开浏览器并起本地服务器，访问 http://127.0.0.1:8090/ |
| ④ 手动启动 | 命令行运行 `node serve.cjs` | 然后浏览器打开 http://127.0.0.1:8090/ |

首次打开页面会白屏一两秒，是浏览器在用内置 Babel 实时编译 JSX，属正常现象。

**能用到的页面（路由）：** `/`（首页）、`#/database`（异常数据库）、`#/news`（新闻）、`#/organizations`（组织）、`#/join`（加入）、`#/admin`（后台）、`#/auth`（登录）等，登录/后台/邮箱等数据都存在浏览器 localStorage，无真实后端。

---

## 二、目录结构

```
娜梅利亚/
├── index.html                  # 唯一入口：全局 CSS 变量 + 按顺序加载所有 JSX
├── components/
│   ├── router.jsx              # 极简 hash 路由（挂到 window 上）
│   ├── auth.jsx                # 登录态（localStorage 模拟）
│   ├── Header.jsx / Footer.jsx # 全站公共组件
│   ├── organizationsData.jsx   # 8 个成员组织的全部数据（改这里）
│   ├── pages/                  # 各路由页面组件
│   │   ├── Home.jsx            # 首页（57KB，内容大头）
│   │   ├── Admin.jsx           # 管理后台（111KB，最大文件）
│   │   ├── AnomalyArchive.jsx  # 异常信息数据库
│   │   └── ……共 20 个页面
├── lib/                        # 本地化的 React 18.3.1 / ReactDOM / Babel 7.29（勿删）
├── serve.cjs                   # 极简本地静态服务器
├── 一键预览.bat                # 一键启动（端口检测 + 开浏览器）
├── 启动网页.bat                # 双击启动
├── 预览-双击打开.html           # 单文件离线预览版（打包快照）
└── thumbnail.png               # 设计稿缩略图
```

---

## 三、技术要点（给要改代码的 AI 看）

1. **无构建工具、无 npm 依赖、没有后端。** 不要 `npm install`、不要重构成 Vite/Webpack——这个项目靠 `index.html` 里 `<script type="text/babel" src="...">` 标签在浏览器中实时编译运行，改完刷新页面即可生效。
2. **组件是全局函数，依赖加载顺序。** 所有 `.jsx` 文件都不写 `import/export`，直接定义 `function XxxPage() {...}`；`index.html` 里的 script 顺序就是依赖顺序（router → auth → Header/Footer → 数据 → 页面 → App）。新增文件必须同时在 `index.html` 加一行 `<script type="text/babel" src="components/...">`。
3. **路由在 `components/App.jsx`**：基于 `location.hash` 字符串匹配分发，`components/router.jsx` 把 `RouterProvider / useRouter / Link` 挂到 `window`。新增页面 = 写新组件 + 在 `App.jsx` 加分支 + 在 `index.html` 注册脚本。
4. **样式两种来源：**
   - 全局 CSS 变量（`--accent-red`、`--bg-deep`、`--font-serif` 等）和通用类（`.container`、`.section`、`.classification`、`.archive-card`…）在 `index.html` 的 `<style>` 里；
   - 页面局部样式用组件内的 `<style>{`...`}</style>` 字符串，改页面时直接在对应组件里找。
5. **内容数据都在组件文件里**：组织数据 → `components/organizationsData.jsx`；新闻列表 → `pages/News.jsx` 与 `components/GuideNews.jsx`；异常档案 → `pages/AnomalyArchive.jsx`、`components/AnomalyFile.jsx` 等；都是普通 JS 数组/对象，直接改中文文案即可。
6. **编码红线：所有文件必须是 UTF-8（无 BOM）。** 用 GBK/ANSI 保存中文会变成乱码。写代码时编辑器统一选 UTF-8。
7. **改完自检：** 命令行执行
   `node -e "require('./lib/babel.min.js');global.Babel.transform(require('fs').readFileSync('components/pages/Home.jsx','utf8'),{presets:['react']})"`
   可快速验证单个 JSX 能否编译（换成对应文件路径即可）；页面级报错看浏览器 F12 控制台。
8. **预览版与源码版不同步：** `预览-双击打开.html` 是交付时的打包快照，AI 改完源码后如需要，可让 AI 重新生成单文件版（把 lib 三个 JS 与全部 JSX 按 index.html 顺序内联），或直接用 `一键预览.bat` 跑源码版预览。

---

## 四、常见改动场景示例

- **改首页大标题文案** → `components/pages/Home.jsx` 里搜索「了解异常」等关键词。
- **换主题色** → `index.html` 顶部 `:root` 里的 `--accent-red` 等变量。
- **增删成员组织** → `components/organizationsData.jsx` 数组。
- **新增一个页面** → 按第三节第 3 条三步走（写组件、加路由分支、注册脚本）。
- **给页面加交互/弹窗** → 在对应页面组件里用 React state（组件内已可直接用 `React.useState`）。

---

*生成日期：2026-08-17 ｜ 原始来源：飞书秒搭导出包 app_17c5vb2nj8s.zip（内容未改动，仅本地化运行环境）*
